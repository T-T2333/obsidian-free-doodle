## Fixed

- **Stroke eraser**: pressing on empty space no longer wastes an undo slot; one drag now counts as a single undo step
- **Anchoring**: editing note text during a drawing session no longer causes ink to jump or lose its anchor (stale text-block cache invalidated, detached blocks skipped)
- **Popout windows**: text anchoring now works when a note is opened in a separate Obsidian window (hit testing uses the canvas' own document)
- Removed dead code

**Full Changelog**: https://github.com/T-T2333/obsidian-free-doodle/compare/1.6.0...1.6.1
